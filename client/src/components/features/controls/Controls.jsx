import React from 'react';
import ControlCompact from './ControlCompact';

const Controls = () => {
  return (
    <div className="row">
      <ControlCompact />
    </div>
  )
};

export default Controls;
